{
  "name": "@onecount/contracts",
  "version": "0.5.1",
  "description": "Pure, framework-free cross-app contracts for the OneCount family (entitlements, deep-link registry, receiving-context schema, id vocabulary, family-activity kinds, product identity). No React Native / Next / RevenueCat deps.",
  "license": "UNLICENSED",
  "main": "./dist/index.js",
  "types": "./dist/index.d.ts",
  "files": [
    "dist"
  ],
  "sideEffects": false,
  "scripts": {
    "build": "tsc -p tsconfig.json"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/onecount/onecount-contracts.git"
  },
  "devDependencies": {
    "typescript": "^5.6.0"
  }
}
